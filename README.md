# BaseView

A description of this package.

## Example App

This repo includes an iOS example app spec that showcases all BaseView view types,
rendered with `UIComponent` from `../UIComponent`.

1. Generate the project:
   `xcodegen generate`
2. Open `BaseViewExample.xcodeproj`
3. Run the `BaseViewExample` scheme

The app deployment target is iOS 26.0.
